services:
  bot:
    build: .
    container_name: voucher_bot
    restart: always
    env_file:
      - .env
    volumes:
      - voucher_data:/app/data
      - voucher_offset:/app/data_offset
    deploy:
      resources:
        reservations:
          cpus: '0.1'
          memory: 128M
        limits:
          cpus: '0.5'
          memory: 512M

volumes:
  voucher_data:
    name: voucher_data
  voucher_offset:
    name: voucher_offset
